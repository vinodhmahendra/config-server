security:
  basic:
    enabled: false # turn off securing our application endpoints

management:
  endpoints:
    web:
      base-path: /manage
      exposure:
        include:
        - * # turn off securing the actuator endpoints
